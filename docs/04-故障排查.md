# 04 · Troubleshooting / 故障排查

| 现象 Symptom | 原因 Cause | 解决 Fix |
|---|---|---|
| 容器反复重启，日志 `listen EADDRINUSE 127.0.0.1:3080` / Container restarts repeatedly, logs `listen EADDRINUSE 127.0.0.1:3080` | 旧版入口让 socat 与 dsh 抢同一端口 / The old entrypoint makes socat and dsh fight over the same port | 确认使用本仓库 entrypoint（socat 听 3080、dsh 听 3081），重建容器 / Make sure you use this repo's entrypoint (socat listens on 3080, dsh on 3081) and rebuild the container |
| 启动报 `plugin tree failed to load` / `node:zlib` 等错误 / Errors at startup like `plugin tree failed to load` / `node:zlib` | 基础镜像 Node 版本过低 / The base image's Node version is too old | 使用本仓库 Dockerfile（`node:24-slim`，DSH 需 Node ≥ 22.18）/ Use this repo's Dockerfile (`node:24-slim`; DSH requires Node ≥ 22.18) |
| 页面能开但 `/api/...` 403 / The page opens but `/api/...` returns 403 | 未登录认证（dsh-remote 场景）/ Not authenticated (dsh-remote scenario) | 确认已登录；首次管理员需 loopback 创建（见 [02](02-认证与远程访问.md)）/ Make sure you are logged in; the first admin must be created via loopback (see [02](02-认证与远程访问.md)) |
| 设置页 `settings are unavailable in this browser` / Settings page shows `settings are unavailable in this browser` | DSH 设计：settings 仅 loopback / DSH design: settings are loopback-only | 不影响使用；改配置用宿主机 curl 调 `/api/settings.mutate`（见下文）/ Not a blocker; change settings with curl from the host via `/api/settings.mutate` (see below) |
| `crypto.randomUUID is not a function` | 非 HTTPS / 非 localhost 访问（浏览器安全上下文）/ Accessing from a non-HTTPS / non-localhost origin (browser secure context) | 用 `localhost` 访问、SSH 隧道或反向代理 HTTPS（见 [02](02-认证与远程访问.md)）/ Use `localhost`, an SSH tunnel, or a reverse proxy with HTTPS (see [02](02-认证与远程访问.md)) |
| 首次启动安装 DSH 很慢 / Installing DSH on first boot is slow | 国内 npm 网络 / Slow npm network in China | `.env` 已默认 `NPM_REGISTRY=https://registry.npmmirror.com`，确认生效 / `.env` already defaults to `NPM_REGISTRY=https://registry.npmmirror.com`; verify it takes effect |
| `docker compose up` 报 `DEEPSEEK_API_KEY` 未设置 / `docker compose up` reports `DEEPSEEK_API_KEY` not set | 未配置 `.env` / `.env` is not configured | `cp .env.example .env` 并填写密钥 / Run `cp .env.example .env` and fill in the key |

## Configure models with curl on the host / 在宿主机上用 curl 配置模型（设置页不可用时）

When the settings page is unavailable, you can read and mutate settings from the host with curl.
设置页不可用时，可在宿主机上用 curl 读取和修改设置。

```bash
# 1. Log in and grab the cookie (only when dsh-remote is enabled; skip if not) / 登录拿 cookie（已启用 dsh-remote 时；未启用则跳过）
curl -s -c /tmp/dsh-cookies.txt -X POST http://127.0.0.1:3080/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"你的用户名","password":"你的密码"}'

# 2. Inspect the current settings / 查看当前设置
curl -s -b /tmp/dsh-cookies.txt -X POST http://127.0.0.1:3080/api/settings.describe \
  -H 'Content-Type: application/json' \
  -d '{"type":"client-request","rpcId":"r1","method":"settings.describe","payload":{}}'

# 3. Mutate the llm-deepseek namespace (e.g. baseURL) / 修改 llm-deepseek 命名空间（如 baseURL）
curl -s -b /tmp/dsh-cookies.txt -X POST http://127.0.0.1:3080/api/settings.mutate \
  -H 'Content-Type: application/json' \
  -d '{"type":"client-request","rpcId":"r2","method":"settings.mutate","payload":{"ns":"llm-deepseek","ops":[{"op":"set","path":["baseURL"],"value":"https://api.deepseek.com"}]}}'
```

## Collect diagnostics / 诊断信息收集

When reporting a problem, please attach: / 反馈问题时，请附上：

```bash
docker ps | grep dsh
docker logs dsh --tail 50
docker exec dsh dsh --version
```
